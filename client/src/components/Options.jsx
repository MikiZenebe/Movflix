import React from "react";

function Options({ children }) {
  return (
    <div>
      Options
      {children}
    </div>
  );
}

export default Options;

/*Childern render it's passed into that component
in this case we will pass notificatons*/
